defaults:
  - simulation: pendulum
  - rendering: headless # gl
  - logging: disabled
  - engine: hybrid_profile_e2e
  - _self_

# Use the recommended hybrid approach for project names
project_name: ${hydra:job.name}
